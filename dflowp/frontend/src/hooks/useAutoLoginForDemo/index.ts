/**
 * Früher: automatischer Demo-Login. Entfernt, damit die Login-Seite sichtbar bleibt
 * (Test-Attrappe mit API-Key-Cookie).
 */
export const useAutoLoginForDemo = () => ({ loading: false as const });
