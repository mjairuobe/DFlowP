import type { FC } from "react";
import Box from "@mui/material/Box";

/** Empty home route: no charts or metrics (template cleanup). */
export const DashboardPage: FC = () => {
  return <Box sx={{ minHeight: "40vh" }} />;
};
