# Thin wrapper on official mongo:7 (pin + optional DFlowP-only labels/init later).
# Image name typically: docker.io/crawlabase/dflowp-mongo
FROM mongo:7

LABEL org.opencontainers.image.description="DFlowP MongoDB (mongo:7 base)"
